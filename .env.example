[TEMPLATE]
# Copy to .env for local builds that need a real public URL. read by build.js to
# fill in the canonical tags, robots.txt and sitemap.xml. Entirely optional: on
# Vercel the domain is detected from the deployment, so you only need this to
# pin a domain yourself.
#
# VITE_SITE_URL=https://example.com
